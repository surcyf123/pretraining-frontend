import { LineChart } from ".";
import type { Meta, StoryObj } from "@storybook/react";

export default {
  title: "LineChart",
  component: LineChart,
} as Meta<typeof LineChart>;

export const Template: StoryObj<typeof LineChart> = {};
